# Server Running
The server can be run in 3 different environments (development, testing, and production) each with it's own configuration settings.
## In development
### Run command
`uv run run.py` if uv is installed else `python3 run.py`